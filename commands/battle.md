---
description: Play a Battle Mode turn
argument-hint: "attack|ball|run"
---

Run `python3 "${CLAUDE_PLUGIN_ROOT}/buddymon.py" battle $ARGUMENTS` and print the output verbatim. If no action was given, say the options are `attack`, `ball`, or `run`.
