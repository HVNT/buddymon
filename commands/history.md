---
description: Browse your buddy's journey journal (catches, evolutions, milestones)
argument-hint: "[count]"
---

Run `python3 "${CLAUDE_PLUGIN_ROOT}/buddymon.py" history $ARGUMENTS` and print the output verbatim.
