---
description: Play a Safari turn against a waiting rare or legendary pokemon
argument-hint: "rock|bait|ball|run"
---

Run `python3 "${CLAUDE_PLUGIN_ROOT}/buddymon.py" safari $ARGUMENTS` and print the output verbatim. If no action was given, say the options are `rock`, `bait`, `ball`, or `run`.
